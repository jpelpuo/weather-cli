// const {validateApiKey, validateUnits, validateCityId, getCurrentWeather, getWeatherForecast} = require('../utils');
// const {configKey} = require('../configure');
const {Conf} = require('conf');

test("Validates the API Key", () => {
    // const config = new Conf().get(configKey);
    // const apiKey = config.apiKey;
    // expect(validateApiKey(apiKey)).toBeTruthy();
    expect(1 + 1).toBe(2);
})