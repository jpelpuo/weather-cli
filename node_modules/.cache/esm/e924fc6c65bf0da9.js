let minimist,version,help,now,forecast,configure;_65b‍.x([["cli",()=>cli]]);_65b‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_65b‍.w("./version",[["version",["version"],function(v){version=v}]]);_65b‍.w("./help",[["help",["help"],function(v){help=v}]]);_65b‍.w("./now",[["now",["now"],function(v){now=v}]]);_65b‍.w("./forecast",[["forecast",["forecast"],function(v){forecast=v}]]);_65b‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);






       async function cli(argsArray){
    const args = minimist(argsArray.slice(2));
    
    let cmd = args._[0] || 'help';

    if(args.version || args.v){
        cmd = 'version';
    }

    if(args.help || args.h){
        cmd = 'help';
    }

    switch(cmd){
        case 'help':
            help(args);
            break;
        case 'version':
            version();
            break;
        case 'now':
            now(args);
            break;
        case 'forecast':
            forecast(args);
            break;
        case 'config':
            configure(args);
            break;
        default:
            console.error(`${cmd} is not a valid command`);
            break;
    }
}